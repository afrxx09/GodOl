<%= form_for @contact_type do |f| %>
	
	<%= render 'shared/error_messages', object: f.object %>
	
	<div class="form-row">
		<div class="form-label"><%= f.label :name, "Typ-namn" %></div>
		<div class="form-input"><%= f.text_field :name %></div>
	</div>
	
	<div class="form-row">
		<%= f.submit "Lägg till kontakt-typ" %>
	</div>
	
<% end %>